{
  "name": "@via-vitae/qa-fixtures",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic fixture data, schemas, generators and PII validator for viavitae-qa.",
  "type": "module",
  "scripts": {
    "generate": "tsx generators/seed.ts",
    "validate": "tsx validator/validate.ts",
    "test": "tsx validator/validate.ts"
  },
  "dependencies": {
    "@faker-js/faker": "^9.0.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
