import { describe, it } from "node:test";
import assert from "node:assert/strict";
describe("Bitrix24 adapter contract", () => {
  it("adapter responses match fixtures/webhooks schemas", () => {
    assert.ok(true, "placeholder — validate adapter output against webhook.schema.json");
  });
});
